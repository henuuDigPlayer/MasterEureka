spring:
  application:
    name: eureka-lindj
server:
  port: 8000
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://192.168.1.41:${server.port}/eureka/